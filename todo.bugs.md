# TODO — Bug backlog

Defects in current behaviour. Distinct from `todo.features.md` (new
capabilities) and `todo.packaging.md` (NuGet/CI/release mechanics).

---

- [ ] **After starting a new prompt, the response window does not fully
  scroll to the end to make the prompt text visible.** Requires a small
  manual scroll from the user. Should auto-scroll to the end automatically
  when a new prompt is submitted.
